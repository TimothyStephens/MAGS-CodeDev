# MAGs-CodeDev direct dependencies
# Install with: pip install -r requirements.txt
# Transitive dependencies are resolved automatically by pip.
#
# For a reproducible pinned snapshot, run:
#   pip freeze > requirements.txt

# CLI & UI
typer>=0.9.0
rich>=13.0.0

# LangChain / LangGraph
langgraph>=0.1.0
langchain-core>=0.1.50
langchain-openai>=0.1.0
langchain-google-genai>=1.0.0
langchain-anthropic>=0.1.0

# LLM providers
openai>=1.0.0
anthropic>=0.18.0
google-generativeai>=0.3.0
google-genai>=1.0.0

# Git & Docker
gitpython>=3.1.40
docker>=7.0.0

# Config & data
pyyaml>=6.0.1
pydantic>=2.5.0

# Retry logic
tenacity>=8.0.0

# HTTP client (retry helpers)
httpx>=0.25.0
